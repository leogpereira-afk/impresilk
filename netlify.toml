[build]
  publish = "."

[functions]
  directory = "netlify/functions"

# Importação automática das O.S do Mubisys, de hora em hora.
[functions."mubisys-sync"]
  schedule = "@hourly"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
